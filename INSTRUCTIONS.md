conda update conda
conda install python=3.5
pip install virtualenv
conda create -n iris python=3.5.1
source activate iris
sudo apt-get install libfreetype6-dev libpng12-dev
pip install -r requirements.txt
